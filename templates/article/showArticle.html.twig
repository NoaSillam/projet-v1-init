{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
<br>
<a href="{{ path('app_article_index') }}" class="btn btn-dark" style="margin-left:3%;">Retour</a>
    <h1 style="text-align:center;">L'Article</h1>
    <div style="margin: auto; width: 80%;">
        <div style="margin-left:auto; margin-right:auto;">
    <table class="table">
        <tbody>
            <tr>
                <th>Id :</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Nom :</th>
                <td>{{ article.Nom }}</td>
            </tr>
            <tr>
                <th style="width:10%;">Annonce :</th>
                <td>{{ article.Annonce }}</td>
            </tr>
            <tr>
                <th>Image :</th>
                <td> <img src="{{asset( 'assets/images/'~article.Image) }}" class="card-img-top" alt="" style="width:30%;"></td>
            </tr>
          {#  <tr>#}
            {#    <th>Modifier :</th>#}
           {#     <td><a class="btn btn-info" href="{{ path('app_article_edit', {'id': article.id}) }}" style="width:60px; height:100%;"> <img src="{{asset( 'assets/images/modifier.png') }}" class="card-img-top" style="width:25px;" alt=""></a></td>#}
           {# </tr>#}
          {#  <tr>#}
          {#      <th>Supprimer :</th>#}
          {#      <td>  {{ include('article/_delete_form.html.twig') }}</td>#}
          {#  </tr>#}
        </tbody>
    </table>

</div>
</div>

   {# <a href="{{ path('app_article_edit', {'id': article.id}) }}">edit</a> #}

    {# {{ include('article/_delete_form.html.twig') }}#}
{% endblock %}
