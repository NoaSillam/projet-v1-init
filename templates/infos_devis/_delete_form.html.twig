<form method="post" action="{{ path('app_infos_devis_delete', {'id': infos_devi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infos_devi.id) }}">
    <button class="btn">Delete</button>
</form>
