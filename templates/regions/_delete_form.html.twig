


<form method="post" action="{{ path('app_regions_delete', {'id': region.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer cette région ``{{ region.Nom }}``?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ region.id) }}">
    <button class="btn btn-danger btn-supprimer" style="width:60px; height:100%;"><img src="{{asset( 'assets/images/delete.png') }}" class="card-img-top" style="width:25px;" alt=""></button>
</form>
