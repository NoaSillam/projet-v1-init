{% extends 'base.html.twig' %}

{% block title %}UserArticle index{% endblock %}

{% block body %}
    <h1>UserArticle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_article in user_articles %}
            <tr>
                <td>{{ user_article.id }}</td>
                <td>
                    <a href="{{ path('app_user_article_show', {'id': user_article.id}) }}">show</a>
                    <a href="{{ path('app_user_article_edit', {'id': user_article.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_user_article_new') }}">Create new</a>
{% endblock %}
