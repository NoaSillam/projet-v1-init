{% extends 'base.html.twig' %}

{% block title %}UserNewsletter index{% endblock %}

{% block body %}
<br>
    <h1 style="text-align:center;">Les clients inscrits à notre newsletter</h1>
    <br>
    <div style="margin: auto; width: 80%;">
       <div style="margin-left:auto; margin-right:auto;">
         <a href="{{ path('app_user_newsletter_new') }}" class="btn btn-secondary">Ajouter un client</a>
<br>
<br>
         <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nom</th>
                <th scope="col">Prenom</th>
                <th scope="col">Adresse Mail</th>
                <th scope="col" style="text-align:center;">actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_newsletter in user_newsletters %}
            <tr>
                <td>{{ user_newsletter.id }}</td>
                <td>{{ user_newsletter.nom }}</td>
                <td>{{ user_newsletter.prenom }}</td>
                <td>{{ user_newsletter.adresseMail }}</td>
           
                    <td style="display: flex; align-items: center; justify-content: center; ">
                    <a class="btn btn-success" href="{{ path('app_user_newsletter_show', {'id': user_newsletter.id}) }}" style="width:60px; height:100%; margin-right: 10px;"><img src="{{asset( 'assets/images/plus.png') }}" class="card-img-top" style="width:25px;" alt=""></a>
                    <a class="btn btn-info" href="{{ path('app_user_newsletter_edit', {'id': user_newsletter.id}) }}" style="width:60px; height:100%; margin-right: 10px;"> <img src="{{asset( 'assets/images/modifier.png') }}" class="card-img-top" style="width:25px;" alt=""></a>    
                    {{ include('user_newsletter/_delete_form.html.twig') }}


{#                    <a href="{{ path('app_user_newsletter_show', {'id': user_newsletter.id}) }}">show</a>#}
{#                    <a href="{{ path('app_user_newsletter_edit', {'id': user_newsletter.id}) }}">edit</a>#} 
               </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>
</div>


{#    <a href="{{ path('app_user_newsletter_new') }}">Create new</a>#}
{% endblock %}
